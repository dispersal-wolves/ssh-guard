# Contributing

Keep changes small, observable, and reversible. New behavior needs a test, user-facing flags need help text, and output fields should remain stable within a minor release.

Before opening a pull request, run the repository's documented format, test, and lint commands. Never commit captured host reports, credentials, private keys, machine identifiers, or production logs.
